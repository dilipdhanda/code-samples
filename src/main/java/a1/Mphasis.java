package a1;

public class Mphasis {
    /*
    [2:51 PM] Amit Ganguly01

2) coding ques-
- write a class to achieve below functionalities
    - to get word count from a given string
    - to talley unique words in the string
    - to talley unique characters in the string

    String[] tokens = inpStr.slpit(" "); // can se regular expression for better word delineation
    HashMap<String,Integer> words = new HashMap<>;
    HashMap<Character,Integer> chars = new HashMap<>;
    for word in Tokens{
        // check work in words hashmap and add a new try with 1 or incr entry for existing entry
        Charcter[] charTokens = String.toArray(word);
        for c in charToken{
            // check c in chars hashmap and add a new try with 1 or incr entry for existing entry
        }
    }

- run above program to execute above functionalities in parallel
    Use ExecutorService with Future and submit eveny charTokens as a job there
        // need to use concurrenthashmap for chars as other Future jobs can access the same data struture

-  merge 2 lists of type numbers and get a list of unique numbers using stream
.match().unique()

     */
}
